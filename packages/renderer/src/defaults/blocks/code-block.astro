---
const { block } = Astro.props
const language = block.language || ""
const code = block.code || ""
---
<pre class="bg-gray-900 text-gray-100 rounded-lg p-4 my-4 overflow-x-auto text-sm">
  <code class={language ? `language-${language}` : ""}>{code}</code>
</pre>
